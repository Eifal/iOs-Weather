// in case you use custom fonts, make you added font file names in customfont.css. avoid empty lines in customfont.css

var useCustomFont=1; // avoid spaces before and after the equal sign "="

var showdailyforecast=0;

var showhourlyforecast=0;

var showbackgroundimage=1; // avoid spaces before and after the equal sign "="
var usedaynightbackgroundimage=0; // avoid spaces before and after the equal sign "="

var showtimedwalls=0; // avoid spaces before and after the equal sign "="

var showweatherwalls=0; // avoid spaces before and after the equal sign "="

var showsunmoon=0; // avoid spaces before and after the equal sign "="

var showoverlay1=1; // avoid spaces before and after the equal sign "="

var showoverlay2=1; // avoid spaces before and after the equal sign "="

var showoverlay3=1; // avoid spaces before and after the equal sign "="

var useBattery=1; // avoid spaces before and after the equal sign "="

var useMaps=0; // avoid spaces before and after the equal sign "="

var useCalendar=1; // avoid spaces before and after the equal sign "="

var showmonthlyCalendar=0; // avoid spaces before and after the equal sign "="

var maxTodaycalentries=0; // avoid spaces before and after the equal sign "="

var maxTomorrowcalentries=0; // avoid spaces before and after the equal sign "="

var useRSS=0; // avoid spaces before and after the equal sign "="
var RSSFeed="https://www.tagesschau.de/xml/rss2/"; // avoid spaces before and after the equal sign "="

var usecoloredtemperature=0;

var showProfilePic=1; // avoid spaces before and after the equal sign "="

// in case you turned on show timed wallpaper above, kindly define hours if day to switch between wallpapers
var hour1greet=5;
var hour2greet=11;
var hour3greet=14;
var hour4greet=17;
var hour5greet=22;

var username="Eifal";

var date1="DD.MM.YY";
var date2="dddd";
var date3="DD";
